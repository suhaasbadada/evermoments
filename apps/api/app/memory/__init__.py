"""Cognee Memory Engine (Module 3).

Ingests structured memory events, stores them as a typed knowledge graph, and
answers questions with provenance and safety warnings. Everything outside this
package talks to it only through the ``app.schemas.memory`` contract.
"""
